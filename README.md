# MJ Scores — Live Football
1. Install Node.js 18+.
2. Run `npm install`.
3. Create `.env.local` in the project root.
4. Add `SPORTS_API_KEY=YOUR_KEY`.
5. Run `npm run dev`.
6. Open http://localhost:3000.

The key stays server-side. The dashboard refreshes every 15 seconds.
